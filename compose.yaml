name: collab
services:
  postgres:
    image: 'postgres:18-alpine'
    environment:
      - 'POSTGRES_DB=collab'
      - 'POSTGRES_USER=dev'
      - 'POSTGRES_PASSWORD=dev'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d collab"]
      interval: 5s
      timeout: 5s
      retries: 5

  collab:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy


volumes:
  postgres_data: